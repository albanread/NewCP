# Fixtures staged at test time (e.g. Empty.odc copied from a local
# BlackBox 1.7-a1 distribution). They're not part of the repository;
# the integration tests stage them on demand and clean up after.
*.odc
