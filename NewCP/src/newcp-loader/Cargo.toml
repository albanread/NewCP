[package]
name = "newcp-loader"
version = "0.1.0"
edition = "2024"

[dependencies]
newcp-llvm = { path = "../newcp-llvm" }
newcp-parser = { path = "../newcp-parser" }
newcp-runtime = { path = "../newcp-runtime" }
newcp-sema = { path = "../newcp-sema" }
