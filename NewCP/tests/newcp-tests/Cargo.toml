[package]
name = "newcp-tests"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
newcp-loader = { path = "../../src/newcp-loader" }
newcp-runtime = { path = "../../src/newcp-runtime" }
